#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    int search(vector<int>& nums, int target) {

    }
};

int main() {
    int a[] = {4,5,6,7,0,1,2};
    vector<int> nums(a,a+7);

    Solution s;
    cout << s.search(nums,1);

    return 0;
}